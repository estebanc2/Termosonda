package com.mtc.termosonda.ui.permissions

import android.Manifest

object PermissionsUtils {
    val permissions =
        listOf(
            Manifest.permission.ACCESS_FINE_LOCATION, Manifest.permission.ACCESS_COARSE_LOCATION
        )
}